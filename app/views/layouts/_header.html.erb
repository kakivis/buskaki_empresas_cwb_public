<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light login-nav">
    <div class="collapse navbar-collapse" id="navbarLogin">
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
        <% if logged_in? %>
          <li class="nav-item active"><%= link_to "Log out", sign_out_path, method: "delete", :class => "nav-link" %></li>
        <% else %>
          <li class="nav-item active"><%= link_to "Login", root_path, :class => "nav-link" %></li>
        <% end %>
      </ul>
    </div>
  </nav>
  <div>
    <h1 style="text-align: center">Buskaki Empresas</h1>
  </div>
</header>