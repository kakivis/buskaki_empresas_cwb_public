<div class="form-busca">
  <h4>Busca por bairro</h4>

  <%= form_with url: filtrar_por_bairro_path, local: true, method: :get do |form| %>
    <%= form.label :query, "Bairro:" %>
    <%= form.text_field :bairro %>
    <%= form.submit "Buscar" %>
  <% end %>
</div>